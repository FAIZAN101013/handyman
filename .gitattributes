# Shell scripts must keep LF endings — a CRLF build.sh fails on Render with
# "bad interpreter: /usr/bin/env bash^M".
*.sh text eol=lf

# Binary assets git should never try to convert
*.png binary
*.jpg binary
*.jfif binary
*.ico binary
